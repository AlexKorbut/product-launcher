{
  "name": "morning-paper-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Composes a RenderDocument + theme into HTML and prints a PDF via Paged.js + Playwright.",
  "bin": { "mp-render": "./render.mjs" },
  "scripts": {
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "pagedjs": "^0.4.3",
    "playwright": "^1.45.0",
    "sharp": "^0.33.0",
    "smol-toml": "^1.2.0"
  }
}
