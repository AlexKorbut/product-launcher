import type { Metadata } from 'next';
import { SITE } from '@/lib/site';

export const metadata: Metadata = {
  title: 'Возможности — автоматизация Telegram-каналов с ИИ',
  description: 'Каналы-доноры, ИИ-рерайт на Claude, анти-плагиат, авто-публикация по расписанию, мультиканальность и оплата за факт. Всё для контент-завода в Telegram.',
  alternates: { canonical: '/features' },
};

const BLOCKS = [
  { t: 'Мониторинг каналов-доноров', d: 'Подключайте любые публичные Telegram-каналы. Userbot читает их инкрементально, дедуплицирует посты и отсекает рекламу по фильтрам — мин. длина, стоп-слова, ключевики.' },
  { t: 'ИИ-рерайт на Claude', d: 'Три уровня переработки: лёгкий рерайт, глубокий рерайт и «по мотивам» (новый пост на тему). Тон, аудитория, язык, хэштеги и запретные темы — всё настраивается на каждый канал.' },
  { t: 'Анти-плагиат', d: 'Каждый рерайт проверяется на похожесть с источником. Слишком близкие тексты автоматически отклоняются — ваш контент остаётся уникальным.' },
  { t: 'Перенос медиа', d: 'Фото из донорских постов скачиваются и переотправляются в ваш канал через Bot API — пост выходит с картинкой, как задумано.' },
  { t: 'Умное расписание', d: 'Задайте число постов в день и тихие часы — система равномерно распределит публикации по таймзоне канала. Flood-wait и ретраи обрабатываются автоматически.' },
  { t: 'Модерация или автопилот', d: 'Включите ручной аппрув перед публикацией или доверьте всё автопилоту — флаг на каждый канал. Редактор постов и календарь контента в комплекте.' },
  { t: 'Мультиканальность для агентств', d: 'Десятки каналов в одном дашборде. Данные изолированы по организациям — клиентские проекты не пересекаются.' },
  { t: 'Прозрачные затраты', d: 'Видно стоимость генерации в токенах и кредитах по каждому посту и каналу. Оплата только за фактически созданный контент.' },
];

export default function Features() {
  return (
    <main className="section">
      <div className="container">
        <h2>Всё для контент-завода в Telegram</h2>
        <p className="sub">От скрапинга доноров до публикации — единый конвейер на ИИ.</p>
        <div className="grid">
          {BLOCKS.map((b) => (
            <div className="feature" key={b.t}><h3>{b.t}</h3><p>{b.d}</p></div>
          ))}
        </div>
        <div style={{ textAlign: 'center', marginTop: 40 }}>
          <a className="btn" href={`${SITE.appUrl}/signup`}>Попробовать бесплатно</a>
        </div>
      </div>
    </main>
  );
}
